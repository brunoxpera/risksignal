// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.31.1
// source: assets.sql

package gen

import (
	"context"

	"github.com/jackc/pgx/v5/pgtype"
)

const upsertAsset = `-- name: UpsertAsset :one

INSERT INTO assets (external_id, source, type, name, environment, criticality, exposure, owner)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
ON CONFLICT (source, external_id) DO UPDATE SET
    type        = EXCLUDED.type,
    name        = EXCLUDED.name,
    environment = EXCLUDED.environment,
    criticality = EXCLUDED.criticality,
    exposure    = EXCLUDED.exposure,
    owner       = EXCLUDED.owner
RETURNING id
`

type UpsertAssetParams struct {
	ExternalID  string
	Source      string
	Type        string
	Name        string
	Environment string
	Criticality string
	Exposure    string
	Owner       pgtype.Text
}

// assets seed write (ARCH-001 §1, WP-1b.05 demo seed).
// UpsertAsset inserts or refreshes an inventory asset by its natural key
// (source, external_id) and returns its id: the demo seed must be
// idempotent, so seeding twice yields no duplicates. created_at stays the
// original registration time; the refresheable fields follow the seed data.
func (q *Queries) UpsertAsset(ctx context.Context, arg UpsertAssetParams) (pgtype.UUID, error) {
	row := q.db.QueryRow(ctx, upsertAsset,
		arg.ExternalID,
		arg.Source,
		arg.Type,
		arg.Name,
		arg.Environment,
		arg.Criticality,
		arg.Exposure,
		arg.Owner,
	)
	var id pgtype.UUID
	err := row.Scan(&id)
	return id, err
}
